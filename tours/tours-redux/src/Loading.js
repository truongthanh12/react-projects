import React from 'react'

const Loading = () => {
  return (
    <div style={style}>Loading...</div>
  )
}
const style = {
    fontSize: "30px",
    textAlign: "center",
    paddingTop: "100px",
    color: "red"
}

export default Loading